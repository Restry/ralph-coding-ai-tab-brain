# Ralph Progress Log
Started: Sat Apr 11 23:15:55 CST 2026
---

## Codebase Patterns
- Use ES module imports in service worker (type: module in manifest)
- manifest.json at repo root, all extension files at root level
- lib/ directory for reusable modules: ai.js, grouper.js, tracker.js, knowledge.js, tabs.js
- Permissions: tabs, tabGroups, sidePanel, storage, activeTab
- Quality checks: `node --check <file>` for syntax, `node -e "JSON.parse(...)"` for manifest
- Chrome Tab Group valid colors: grey, blue, red, yellow, green, pink, purple, cyan, orange
- chrome.storage.sync keys: apiKey, apiEndpoint, model (set by options page)
- Default API endpoint: https://api.anthropic.com, default model: claude-sonnet-4-20250514
- lib/ai.js exports sendMessage(systemPrompt, userMessage) → returns response text string
- AI client uses anthropic-version: 2023-06-01, max_tokens: 1024
- AI client has 10s timeout via AbortController
- lib/tabs.js exports getAllTabs() → returns array of {index, tabId, title, url, domain, favIconUrl, pinned, windowId}
- Domain extraction: new URL(url).hostname, handles data: URLs and invalid URLs gracefully
- lib/grouper.js exports groupTabsWithAI(tabs) → returns {groups, totalGrouped, skipped}
- AI prompt instructs JSON-only response; parseAIResponse() strips markdown fences and validates
- chrome.tabs.group({tabIds}) returns a groupId, then chrome.tabGroups.update(groupId, {title, color})
- After AI call, re-query chrome.tabs.query({}) to catch tabs closed/opened during the request
- Unassigned non-pinned tabs go to a 'Misc' group (grey color)
- lib/grouper.js exports groupTabsByDomain(tabs) for offline domain-based grouping
- lib/grouper.js exports groupTabs() as the main public API (AI first, domain fallback)
- Domain grouping color rotation: blue, green, cyan, orange, purple, pink, red, yellow
- eTLD+1 extraction: handle multi-part TLDs (co.uk, com.au) via KNOWN_SLDS set

## 2026-04-11 23:17 - US-001
- Created project scaffold with Manifest V3
- Files created: manifest.json, service-worker.js, sidepanel.html, sidepanel.js, sidepanel.css, options.html, options.js, options.css, lib/ai.js, lib/grouper.js, lib/tracker.js, lib/knowledge.js
- **Learnings for future iterations:**
  - Manifest V3 uses `"type": "module"` in background.service_worker config for ES module support
  - side_panel config uses `default_path` key
  - options_page is a top-level string key (not nested object)
---

## 2026-04-11 23:30 - US-002
- Implemented options page with API key, endpoint URL, and model configuration
- Files changed: options.html, options.js, options.css
- Form saves/loads settings to/from chrome.storage.sync
- Validation: API key required, endpoint must be valid URL
- Success/error feedback with auto-dismiss after 3s
- Dark theme styling consistent with extension aesthetic
- **Learnings for future iterations:**
  - Storage keys used: `apiKey`, `apiEndpoint`, `model` in chrome.storage.sync
  - Default endpoint: `https://api.anthropic.com`, default model: `claude-sonnet-4-20250514`
  - Trailing slashes stripped from endpoint URL before saving
  - options.js is NOT an ES module (loaded via script tag, not type=module) - uses chrome.* APIs directly from DOM context
---

## 2026-04-11 23:45 - US-003
- Implemented AI client module in lib/ai.js
- Files changed: lib/ai.js
- Exports sendMessage(systemPrompt, userMessage) function
- Reads API key, endpoint, model from chrome.storage.sync
- POST to {endpoint}/v1/messages with x-api-key, anthropic-version, content-type headers
- 10s timeout via AbortController with descriptive error
- Descriptive errors for: missing API key, non-OK response (includes status + body), timeout, unexpected response format
- **Learnings for future iterations:**
  - lib/ai.js is an ES module (uses export), importable by service-worker.js via static import
  - Claude Messages API format: system as top-level string param, messages as array of {role, content}
  - AbortController: setTimeout to call controller.abort(), catch err.name === 'AbortError'
  - Always clearTimeout in both success and error paths to avoid leaks
---

## 2026-04-11 - US-004
- Implemented tab metadata collection module in lib/tabs.js
- Files created: lib/tabs.js
- Exports getAllTabs() function that queries chrome.tabs.query({}) for all tabs
- Returns array of {index (0-based position), tabId, title, url, domain, favIconUrl, pinned, windowId}
- extractDomain() helper handles edge cases: data: URLs, invalid URLs, empty strings
- Pinned tabs included with pinned: true flag
- **Learnings for future iterations:**
  - lib/tabs.js is an ES module, importable by service-worker.js and lib/grouper.js
  - index is a 0-based position assigned by us (via .map index), not Chrome's tab.index
  - Use new URL(url).hostname for domain extraction; data: URLs return '' (no hostname)
  - chrome.tabs.query({}) returns all tabs across all windows
---

## 2026-04-11 - US-005
- Implemented AI-powered tab grouping logic in lib/grouper.js
- Files changed: lib/grouper.js
- Exports groupTabsWithAI(tabs) function
- Constructs prompt with tab metadata (index, title, url, pinned), sends via sendMessage()
- Parses AI JSON response with validation and markdown fence stripping
- Re-queries tabs after AI call to handle closed/opened tabs during the request
- Applies groups via chrome.tabs.group() + chrome.tabGroups.update()
- Unassigned non-pinned tabs go to 'Misc' group (grey)
- Returns summary: {groups: [{name, color, tabCount}], totalGrouped, skipped}
- **Learnings for future iterations:**
  - AI can return markdown-fenced JSON (```json...```), must strip fences before parsing
  - chrome.tabs.group({tabIds: [...]}) creates a group and returns the groupId
  - chrome.tabGroups.update(groupId, {title, color}) sets the visual properties
  - Always validate AI color values against the VALID_COLORS list, default to 'grey'
  - Re-querying tabs after AI call is essential since tabs can close during the 10s timeout
---

## 2026-04-11 - US-006
- Implemented offline fallback grouping by domain in lib/grouper.js
- Added groupTabsByDomain(tabs): groups by eTLD+1, color rotation, single-tab domains go to 'Misc'
- Added groupTabs(): main public API that tries AI first, falls back to domain on any error
- Added getETLDPlusOne() helper with multi-part TLD handling (co.uk, com.au, etc.)
- Files changed: lib/grouper.js
- **Learnings for future iterations:**
  - groupTabs() is now the main entry point; callers should use this instead of groupTabsWithAI() directly
  - Domain grouping uses console.warn (not console.error) for fallback notification
  - getAllTabs is now imported in grouper.js (needed for groupTabs() to get fresh tab data)
  - eTLD+1 approximation works for common cases; a full PSL library would be needed for edge cases
---
